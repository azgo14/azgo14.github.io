# Andrew Zhai Personal Website

A simple static personal website for Andrew Zhai.

## Local Preview

Open `index.html` in a browser, or serve the folder locally:

```sh
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
